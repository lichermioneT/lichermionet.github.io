# 博客写作与发布指南

博客仓库位置：

```text
D:\Desktop\lic
```

文章存放位置：

```text
D:\Desktop\lic\source\_posts
```

## 一、新建博客文章

在 `source/_posts` 文件夹中新建一个 `.md` 文件，例如：

```text
C语言指针详解.md
```

文件建议使用有意义的中文名称，并保存为 UTF-8 编码。

每篇文章开头需要填写 Front Matter：

```yaml
---
title: C 语言指针详解
date: 2026-07-29 20:30:00
updated: 2026-07-29 20:30:00
description: 系统讲解 C 语言指针、指针运算、数组指针以及常见错误。
categories:
  - C 语言
tags:
  - C 语言
  - 指针
  - 内存管理
comments: false
---

这里开始写文章正文。

## 一、指针的基本概念

正文内容……
```

字段说明：

- `title`：网站上显示的文章标题。
- `date`：文章首次发布时间。
- `updated`：文章最后修改时间。
- `description`：文章摘要，用于首页展示和搜索引擎收录。
- `categories`：文章所属专栏，通常只填写一个。
- `tags`：文章关键词，可以填写多个。
- `comments`：是否开启评论，目前统一设置为 `false`。

## 二、可使用的专栏分类

`categories` 必须使用下面的名称之一，并保持文字完全一致：

```text
C++
Linux
C 语言
数据结构
设计模式
读书
```

例如：

```yaml
categories:
  - 数据结构
tags:
  - 数据结构
  - 二叉树
  - 遍历
```

## 三、关于文章图片

目前博客文章不使用本地图片。

从其他笔记复制文章时，需要删除类似下面的图片引用：

```markdown
![图片说明](./picture/xxx.png)
```

否则图片文件不存在时，博客检查和 GitHub Pages 部署会失败。

## 四、本地预览

打开 Git Bash，进入博客仓库：

```bash
cd /d/Desktop/lic
```

启动本地博客：

```bash
npm run server
```

浏览器访问：

```text
http://localhost:4000
```

检查标题、目录、代码块和正文排版。预览完成后，在终端按 `Ctrl + C` 关闭服务器。

## 五、发布前检查

在仓库目录执行：

```bash
npm run validate
```

看到下面的提示才可以继续发布：

```text
站点检查通过
```

如果检查失败，需要根据终端中的错误信息修改文章。

## 六、提交并发布博客

每次发布前，先同步远程仓库：

```bash
git pull --ff-only
```

然后检查、提交并推送：

```bash
git status
git add -A
git diff --cached
git commit -m "post: 新增 C 语言指针文章"
git push
```

推送后，GitHub Actions 会自动构建并发布博客。通常等待 1～2 分钟即可访问：

```text
https://lichermionet.github.io/
```

可以在下面的页面检查部署状态：

```text
https://github.com/lichermioneT/lichermionet.github.io/actions
```

最新的 `Deploy Hexo to GitHub Pages` 任务显示绿色，表示发布成功。

## 七、修改已有文章

直接修改 `source/_posts` 中对应的 `.md` 文件，并更新文章头部的 `updated`：

```yaml
updated: 2026-07-30 10:00:00
```

然后重新检查和发布：

```bash
npm run validate
git add -A
git commit -m "post: 更新文章内容"
git push
```

不要修改原来的 `date`，否则文章的发布时间和链接可能发生变化。

## 八、换电脑后继续更新博客

换电脑时不需要复制整个 `node_modules` 或 `public` 文件夹。博客源文件已经保存在 GitHub，只需要在新电脑上重新克隆仓库并安装依赖。

### 1. 离开旧电脑前

先在旧电脑上确认所有文章都已经提交并推送：

```bash
cd /d/Desktop/lic
git status
npm run validate
git add -A
git commit -m "chore: 同步换电脑前的博客修改"
git push
```

如果 `git status` 显示：

```text
nothing to commit, working tree clean
```

说明没有未提交的文件，可以直接使用新电脑。

如果旧电脑已经损坏，只要之前执行过 `git push`，已经推送到 GitHub 的文章仍然可以恢复。没有推送的本地文件无法从 GitHub 找回。

### 2. 在新电脑安装环境

需要安装：

```text
Git
Node.js 24
```

Node.js 安装时会同时安装 npm。

安装完成后打开 Git Bash，检查版本：

```bash
git --version
node --version
npm --version
```

### 3. 配置 Git 身份

第一次使用 Git 时执行：

```bash
git config --global user.name "lichermioneT"
git config --global user.email "你的 GitHub 邮箱"
```

查看配置是否生效：

```bash
git config --global --list
```

### 4. 克隆博客仓库

进入准备存放博客的目录，例如桌面：

```bash
cd /d/Desktop
```

从 GitHub 下载完整博客源文件：

```bash
git clone https://github.com/lichermioneT/lichermionet.github.io.git lic
cd lic
```

不要先手动创建一个普通的 `lic` 文件夹再执行 `git init`。直接使用 `git clone`，它会自动创建带有 `.git` 的正确仓库。

### 5. 恢复项目依赖

进入新电脑上的博客目录后执行：

```bash
npm ci
```

这个命令会根据 `package-lock.json` 自动恢复博客需要的依赖。不要从旧电脑复制 `node_modules`。

安装完成后检查项目：

```bash
npm run validate
```

如果显示“站点检查通过”，说明新电脑的博客环境已经恢复完成。

### 6. 在新电脑预览博客

```bash
npm run server
```

浏览器打开：

```text
http://localhost:4000
```

预览完成后按 `Ctrl + C` 关闭。

### 7. 新电脑第一次推送

使用 HTTPS 克隆时，第一次执行 `git push` 可能会弹出 GitHub 登录窗口。按照提示通过浏览器登录并授权即可，不要在终端中填写 GitHub 账户密码。

完成文章修改后执行：

```bash
git pull --ff-only
npm run validate
git add -A
git diff --cached
git commit -m "post: 新增文章"
git push
```

### 8. 多台电脑之间同步

如果以后会在多台电脑上写博客，请遵守下面的顺序：

```text
开始写作前：git pull --ff-only
完成写作后：npm run validate
然后提交：git add -A
创建提交：git commit
最后同步：git push
```

不要在两台电脑上同时修改同一篇文章。每次换到另一台电脑前，都要先在当前电脑执行 `git push`；在另一台电脑开始工作前，再执行 `git pull --ff-only`。

如果 `git pull --ff-only` 提示无法更新，不要强行执行 `git reset --hard`，先使用：

```bash
git status
git log --oneline --decorate -5
```

检查是否存在未提交修改或两台电脑产生了不同提交，再决定如何合并。

### 9. 换电脑后的完整流程

```text
旧电脑提交并 git push
→ 新电脑安装 Git 和 Node.js 24
→ git clone 博客仓库
→ npm ci
→ npm run validate
→ 开始添加或修改文章
→ git add、commit、push
→ 等待 GitHub Actions 发布
```

## 九、不要上传或修改的内容

以下文件或文件夹由程序自动生成，不需要手动上传：

```text
node_modules
public
db.json
.deploy_git
.cache
```

也不需要执行：

```bash
hexo deploy
```

当前仓库只需要通过 `git push` 发布。

## 十、最简发布流程

```text
在 source/_posts 中添加或修改文章
→ 填写正确的 Front Matter
→ 删除无效图片引用
→ npm run validate
→ git pull --ff-only
→ git add -A
→ git commit
→ git push
→ 等待 GitHub Actions 部署成功
```
